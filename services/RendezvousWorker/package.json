{
  "name": "@opensteamer/rendezvous-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "check": "wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "dev": "wrangler dev",
    "test": "vitest run",
    "deploy": "wrangler deploy",
    "smoke:public": "node scripts/smoke-public.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.18.4",
    "vitest": "4.1.10",
    "wrangler": "4.110.0"
  }
}
